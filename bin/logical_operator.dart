void main() {
  /**
   * logical operator
   * ||, &&, !
   */

  var negation = false;
  print(!negation);

  var score = 80;
  var exam = 90;

  if (score > 70 && exam > 80) {
    print('Passed!');
  } else {
    print('Failed!');
  }
}
